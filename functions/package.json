{
  "name": "receiptai-pro-functions",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json -w",
    "serve": "npx firebase-tools emulators:start --only functions,firestore,storage",
    "deploy": "npx firebase-tools deploy --only functions"
  },
  "engines": { "node": "20" },
  "dependencies": {
    "dotenv": "^16.4.5",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
