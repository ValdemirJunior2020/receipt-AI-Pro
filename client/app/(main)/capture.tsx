import { View, Text } from 'react-native';
export default function Capture() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Capture screen (Step 3)</Text>
    </View>
  );
}
